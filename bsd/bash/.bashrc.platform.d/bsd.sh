# shellcheck shell=bash
# ~/.bashrc.platform.d/bsd.sh — FreeBSD/other BSD overrides (stowed from bsd/bash)

# BSD ls colors (BSD ls uses LSCOLORS, not LS_COLORS)
export LSCOLORS=gxfxdxdxcxegedabadbxgx

# The bash package and the /etc/shells + chsh login-shell setup are handled by setup.sh.