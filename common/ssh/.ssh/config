# ~/.ssh/config — Shared SSH client config

# OrbStack 'orb' host (must precede any Host block; silently ignored if absent)
Include ~/.orbstack/ssh/config

Host *
    AddKeysToAgent yes
    IdentitiesOnly yes
    ServerAliveInterval 60
    ServerAliveCountMax 3
