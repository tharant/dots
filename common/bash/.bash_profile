# ~/.bash_profile — Login shell entry point
# Sourced by: SSH sessions, macOS Terminal tabs, `bash -l`
# Convention: keep this minimal, source .bashrc for interactive config

# Source .bashrc for interactive shell setup
[[ -f ~/.bashrc ]] && source ~/.bashrc
