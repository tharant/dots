# {{PROJECT_NAME}}

Scaffolded from the `example` template in the dots repo.
Placeholder content only — replace with real project docs.

Runtimes come from `.runtimesrc` at the project root (see
docs/direnv-runtimes.md); `direnv allow` activates them on `cd`.